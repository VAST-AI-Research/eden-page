(function(){
"use strict";
var palette=["#d7ff4f","#4fe5d2","#ff8a3d","#8c78ff","#ff5f8f","#5f8cff","#ffc857","#78e08f"];
// Stage 2 Interactive 3D Scene defaults. Camera offsets are multipliers of
// the scene bounding-box radius and are added to its centre.
var stage2ViewerDefaults={
  panoramaRotationYDegrees:90,
  cameraFovDegrees:45,
  cameraOffsetRadius:{x:0,y:0.2,z:1.0},
  nearRadiusScale:1/1000,
  farRadiusScale:30
};
var stageNames=["Segmentation","Inference CCM + Mesh","Construct Scene"];
var state={cases:[],caseId:null,caseData:null,stage:0,review:null,image:null,objects:[],selectedId:null,tool:"select",points:[],labels:[],box:null,boxStart:null,drawing:false,opacity:.58,depthOverlayOpacity:.58,environmentRotationYDegrees:stage2ViewerDefaults.panoramaRotationYDegrees,stage2EnvironmentScene:null,brushSize:24,candidates:[],candidateIndex:0,undo:[],redo:[],zoom:1,panX:0,panY:0,view:null,panning:false,panStart:null,spacePressed:false,cursorPoint:null,three:null,cleanup:null,overlayCleanup:null,ccmCleanup:null,ccmImageCleanup:null,ccmViewers:[],graphCleanup:null,graphPayload:null,graphOriginal:null,selectedGraphNodeId:null,graphDirty:false,graphZoom:1,stage1Results:{ccm:true,mesh:true},constructOptions:{estimation_depth:false,estimate_floor:false,generate_environment_map:false},pipelineJob:null,pipelinePoll:null,pipelineLogOffset:0};
var environmentRotationStorageKey="mira-scene-environment-rotation-v1";
var byId=function(id){return document.getElementById(id)},canvas=byId("sceneCanvas"),ctx=canvas.getContext("2d"),maskCanvas=byId("selectedMaskCanvas"),maskCtx=maskCanvas.getContext("2d"),paintCursor=byId("paint-cursor"),selectedMaskCursor=byId("selected-mask-cursor");
function esc(v){return String(v==null?"":v).replace(/[&<>"']/g,function(c){return{"&":"&amp;","<":"&lt;",">":"&gt;",'"':"&quot;","'":"&#39;"}[c]})}
async function api(url,options){var r=await fetch(url,options||{}),d={};try{d=await r.json()}catch(_){}if(!r.ok)throw new Error(d.detail||d.error||("HTTP "+r.status));return d}
function json(method,body){return{method:method,headers:{"Content-Type":"application/json"},body:JSON.stringify(body||{})}}
function toast(message,error){var n=document.createElement("div");n.className="toast"+(error?" error":"");n.textContent=message;byId("toasts").append(n);setTimeout(function(){n.remove()},4500)}
function setSystem(label,kind){byId("systemState").textContent=label;byId("stateDot").className="state-dot "+(kind||"")}
function route(){var q=new URLSearchParams(location.search),s=Number(q.get("stage")||0);return{caseId:q.get("case"),stage:Number.isInteger(s)?Math.max(0,Math.min(stageNames.length-1,s)):0}}
function updateUrl(replace){var q=new URLSearchParams({case:state.caseId||"",stage:String(state.stage)});history[replace?"replaceState":"pushState"]({},"","?"+q)}
function normalizeCaseData(data){data=data||{};var stages=data.stages||[],byKey=new Map(stages.map(function(stage){return[stage&&stage.key,stage]})),seg=byKey.get("segmentation")||stages[0]||{key:"segmentation",label:"Segmentation"},ccm=byKey.get("inference_ccm_mesh"),legacyCcm=byKey.get("inference_ccm"),legacyMesh=byKey.get("inference_mesh"),construct=byKey.get("construct_scene"),environmentFile=[].concat.apply([],stages.map(function(stage){return stage&&stage.files||[]})).find(function(file){return /(?:^|\/)(?:Environment|environment)\/environment_equirect\.(?:png|jpe?g|webp|hdr|exr)$/i.test(file.name||"")});if(!data.environment_url&&environmentFile)data.environment_url=environmentFile.url;if(!ccm&&(legacyCcm||legacyMesh)){ccm=Object.assign({},legacyCcm||legacyMesh,{key:"inference_ccm_mesh",label:"Inference CCM + Mesh",ready:Boolean(legacyCcm&&legacyCcm.ready||legacyMesh&&legacyMesh.ready),summary:(legacyCcm&&legacyCcm.summary||"No CCM")+" · "+(legacyMesh&&legacyMesh.summary||"No mesh"),files:[].concat(legacyCcm&&legacyCcm.files||[],legacyMesh&&legacyMesh.files||[]),items:(legacyCcm&&legacyCcm.items)||[]})}ccm=ccm||{key:"inference_ccm_mesh",label:"Inference CCM + Mesh",ready:false,files:[],items:[]};construct=construct||{key:"construct_scene",label:"Construct Scene",ready:false,files:[],methods:[]};data.stages=[Object.assign({},seg,{id:0,key:"segmentation"}),Object.assign({},ccm,{id:1,key:"inference_ccm_mesh"}),Object.assign({},construct,{id:2,key:"construct_scene"})];return data}
function renderPipeline(){var root=byId("pipeline");root.replaceChildren();stageNames.forEach(function(name,i){var stage=state.caseData&&state.caseData.stages&&state.caseData.stages[i];var li=document.createElement("li");li.className="workflow-step"+(i===state.stage?" active":"")+(stage&&stage.ready?" complete":"");li.setAttribute("aria-current",i===state.stage?"step":"false");li.innerHTML="<span>"+i+"</span><strong>"+esc(name)+"</strong>";li.onclick=function(){if(!state.caseId){toast("Select a case first",true);return}state.stage=i;updateUrl(false);renderStage()};root.append(li)})}
function renderCases(){var root=byId("scene-thumbnails");root.replaceChildren();byId("case-count").textContent=String(state.cases.length);state.cases.forEach(function(item){var b=document.createElement("button"),im=document.createElement("img"),label=document.createElement("span");b.type="button";b.className="case-item";b.dataset.caseId=item.case_id;b.setAttribute("role","option");b.setAttribute("aria-selected",item.case_id===state.caseId?"true":"false");im.loading="lazy";im.src=item.input_image||"";im.alt=item.case_id+" input preview";im.onerror=function(){im.classList.add("load-error");im.alt="Preview unavailable"};label.textContent=item.case_id;b.append(im,label);b.onclick=function(){openCase(item.case_id,false)};root.append(b)})}
function imageFrom(url){return new Promise(function(resolve,reject){var im=new Image();im.onload=function(){resolve(im)};im.onerror=function(){reject(new Error("Failed to load image"))};im.src=url})}
async function maskFrom(b64,w,h){var im=await imageFrom("data:image/png;base64,"+b64),c=document.createElement("canvas");c.width=w;c.height=h;var x=c.getContext("2d");x.drawImage(im,0,0,w,h);var p=x.getImageData(0,0,w,h);for(var i=0;i<p.data.length;i+=4)p.data[i+3]=p.data[i];x.putImageData(p,0,0);return c}
function setSelectedMaskPaneVisible(visible){var workspace=byId("segmentation-workspace"),pane=document.querySelector(".selected-mask-pane");workspace.style.gridTemplateColumns=visible?"minmax(0,1fr) minmax(0,1fr)":"minmax(0,1fr)";pane.hidden=!visible;pane.setAttribute("aria-hidden",visible?"false":"true");pane.style.display=visible?"":"none"}
async function loadReview(){try{var r=await api("/api/demo/cases/"+encodeURIComponent(state.caseId)+"/review"),im=await imageFrom(r.image_url+"?v="+encodeURIComponent(r.revision)),objects=[];for(var i=0;i<r.objects.length;i++){var o=Object.assign({},r.objects[i]);o._canvas=await maskFrom(o.mask_b64,r.image.width,r.image.height);o._visible=true;objects.push(o)}state.review=r;state.image=im;state.objects=objects;state.selectedId=objects.length?objects[0].id:null;setSelectedMaskPaneVisible(true);resetCanvasView();state.undo=[];state.redo=[];syncHistory();resetPrompts();canvas.style.display="block";byId("emptyStage").classList.add("hidden");byId("save-review").disabled=false;byId("next-stage").disabled=false;byId("canvasMeta").textContent=state.caseId+" · "+r.image.width+"×"+r.image.height;byId("result-summary").textContent=objects.length+" masks ready";renderObjects();requestAnimationFrame(draw)}catch(e){state.review=null;state.objects=[];state.image=null;state.view=null;setSelectedMaskPaneVisible(false);canvas.style.display="none";byId("emptyStage").classList.remove("hidden");byId("save-review").disabled=true;byId("next-stage").disabled=!state.caseId;renderObjects();toast(e.message,true)}}
function selected(){return state.objects.find(function(o){return o.id===state.selectedId})||null}
function selectObject(id,preserveList){state.selectedId=id;resetPrompts();if(preserveList){document.querySelectorAll(".object-item").forEach(function(row){row.classList.toggle("selected",row.dataset.objectId===id)});var o=selected();byId("edit-target").textContent=o?"Editing: "+o.name:"No mask selected";byId("edit-hint").textContent=o?"Selected mask is highlighted on the canvas. Refine it with SAM3 or paint tools.":"Select a mask, then refine it with SAM3 or paint tools."}else renderObjects();draw()}
function renderObjects(){var root=byId("object-list");root.replaceChildren();byId("object-count").textContent=String(state.objects.length);byId("review-progress").textContent="0 / "+state.objects.length+" reviewed";var oNow=selected();byId("finish-mask").disabled=!oNow;byId("edit-target").textContent=oNow?"Editing: "+oNow.name:"No mask selected";byId("edit-hint").textContent=oNow?"Selected mask is highlighted on the canvas. Refine it with SAM3 or paint tools.":"Select a mask, then refine it with SAM3 or paint tools.";if(!state.objects.length){root.innerHTML='<div class="empty-small">No masks are available for this case.</div>';return}state.objects.forEach(function(o,i){var row=document.createElement("div");row.className="object-item"+(o.id===state.selectedId?" selected":"")+(o._visible===false?" mask-hidden":"");row.dataset.objectId=o.id;var sw=document.createElement("span");sw.className="swatch";sw.style.background=palette[i%palette.length];var input=document.createElement("input");input.className="object-name";input.value=o.name;input.title=o.name;input.setAttribute("aria-label","Object name");input.oninput=function(){o.name=input.value;input.title=o.name;if(state.selectedId===o.id){byId("selected-mask-name").textContent=o.name||"Unnamed mask";byId("edit-target").textContent="Editing: "+(o.name||"Unnamed mask")}};input.onclick=function(e){e.stopPropagation()};input.onfocus=function(){if(state.selectedId!==o.id)selectObject(o.id,true)};var actions=document.createElement("div");actions.className="object-actions";var vis=document.createElement("button");vis.type="button";vis.textContent=o._visible===false?"○":"◉";vis.title=o._visible===false?"Show mask":"Hide mask";vis.onclick=function(e){e.stopPropagation();o._visible=o._visible===false;row.classList.toggle("mask-hidden",o._visible===false);vis.textContent=o._visible===false?"○":"◉";draw()};var del=document.createElement("button");del.type="button";del.textContent="×";del.title="Delete mask";del.onclick=function(e){e.stopPropagation();state.objects=state.objects.filter(function(x){return x!==o});if(state.selectedId===o.id)state.selectedId=state.objects[0]?state.objects[0].id:null;renderObjects();draw()};actions.append(vis,del);row.append(sw,input,actions);row.onclick=function(e){if(e.target!==input)selectObject(o.id)};root.append(row)})}
function resetPrompts(){state.points=[];state.labels=[];state.box=null;state.boxStart=null;state.drawing=false;state.candidates=[];renderCandidates();updatePromptCounts()}
function updatePromptCounts(){byId("positive-count").textContent=String(state.labels.filter(function(x){return x===1}).length);byId("negative-count").textContent=String(state.labels.filter(function(x){return x===0}).length);byId("box-count").textContent=state.box?"1":"0"}
function renderCandidates(){var strip=byId("candidate-strip"),root=byId("candidate-buttons");root.replaceChildren();strip.classList.toggle("hidden",!state.candidates.length);byId("candidate-count").textContent=String(state.candidates.length);byId("candidate-active").textContent=state.candidates.length?"selected "+(state.candidateIndex+1):"";state.candidates.forEach(function(c,i){var b=document.createElement("button");b.type="button";b.className=i===state.candidateIndex?"active":"";b.textContent=String(i+1);b.onclick=function(){state.candidateIndex=i;selected()._canvas=c;renderCandidates();draw()};root.append(b)})}
function fitView(){if(!state.image||!state.review)return null;var r=canvas.getBoundingClientRect(),margin=24,scale=Math.min(Math.max(1,r.width-margin*2)/state.review.image.width,Math.max(1,r.height-margin*2)/state.review.image.height)*state.zoom,w=state.review.image.width*scale,h=state.review.image.height*scale;return{x:(r.width-w)/2+state.panX,y:(r.height-h)/2+state.panY,width:w,height:h,scale:scale}}
function updateZoomLabel(){byId("reset-zoom").textContent=Math.round(state.zoom*100)+"%"}
function resetCanvasView(){state.zoom=1;state.panX=0;state.panY=0;state.view=null;updateZoomLabel();draw()}
function setCanvasZoom(nextZoom,anchorX,anchorY){if(!state.review)return;var r=canvas.getBoundingClientRect(),current=state.view||fitView(),x=anchorX===undefined?r.width/2:anchorX,y=anchorY===undefined?r.height/2:anchorY,imageX=(x-current.x)/current.scale,imageY=(y-current.y)/current.scale;state.zoom=Math.max(.5,Math.min(8,nextZoom));var next=fitView();state.panX+=x-(next.x+imageX*next.scale);state.panY+=y-(next.y+imageY*next.scale);updateZoomLabel();draw()}
function point(event){if(!state.view||!state.review)return null;var r=canvas.getBoundingClientRect(),x=(event.clientX-r.left-state.view.x)/state.view.scale,y=(event.clientY-r.top-state.view.y)/state.view.scale;if(x<0||y<0||x>=state.review.image.width||y>=state.review.image.height)return null;return[x,y]}
function tint(mask,color){var c=document.createElement("canvas");c.width=mask.width;c.height=mask.height;var x=c.getContext("2d");x.fillStyle=color;x.fillRect(0,0,c.width,c.height);x.globalCompositeOperation="destination-in";x.drawImage(mask,0,0);return c}
function draw(){if(state.stage!==0)return;drawSelectedMask();if(!state.image||!state.review)return;var r=canvas.getBoundingClientRect(),ratio=window.devicePixelRatio||1,w=Math.max(1,Math.round(r.width*ratio)),h=Math.max(1,Math.round(r.height*ratio));if(canvas.width!==w||canvas.height!==h){canvas.width=w;canvas.height=h}ctx.setTransform(ratio,0,0,ratio,0,0);ctx.clearRect(0,0,r.width,r.height);state.view=fitView();var v=state.view;if(!v)return;ctx.drawImage(state.image,v.x,v.y,v.width,v.height);state.objects.forEach(function(o,i){if(o._visible===false)return;ctx.save();ctx.globalAlpha=o.id===state.selectedId?Math.min(1,state.opacity+.17):state.opacity;ctx.drawImage(tint(o._canvas,palette[i%palette.length]),v.x,v.y,v.width,v.height);ctx.restore()});state.points.forEach(function(p,i){var x=v.x+p[0]*v.scale,y=v.y+p[1]*v.scale;ctx.beginPath();ctx.arc(x,y,6,0,Math.PI*2);ctx.fillStyle=state.labels[i]?"#4fe5d2":"#ff5f68";ctx.fill();ctx.lineWidth=2;ctx.strokeStyle="#0b0d10";ctx.stroke();ctx.fillStyle="#0b0d10";ctx.font="bold 10px sans-serif";ctx.textAlign="center";ctx.textBaseline="middle";ctx.fillText(state.labels[i]?"+":"−",x,y+.5)});if(state.box){ctx.save();ctx.strokeStyle="#d7ff4f";ctx.lineWidth=2;ctx.setLineDash([7,5]);ctx.strokeRect(v.x+state.box[0]*v.scale,v.y+state.box[1]*v.scale,(state.box[2]-state.box[0])*v.scale,(state.box[3]-state.box[1])*v.scale);ctx.restore()}updatePaintCursor()}
function selectedMaskView(){if(!state.review)return null;var r=maskCanvas.getBoundingClientRect(),margin=24,scale=Math.min(Math.max(1,r.width-margin*2)/state.review.image.width,Math.max(1,r.height-margin*2)/state.review.image.height),width=state.review.image.width*scale,height=state.review.image.height*scale;return{x:(r.width-width)/2,y:(r.height-height)/2,width:width,height:height,scale:scale}}
function drawSelectedMask(){var r=maskCanvas.getBoundingClientRect(),ratio=window.devicePixelRatio||1,w=Math.max(1,Math.round(r.width*ratio)),h=Math.max(1,Math.round(r.height*ratio));if(maskCanvas.width!==w||maskCanvas.height!==h){maskCanvas.width=w;maskCanvas.height=h}maskCtx.setTransform(ratio,0,0,ratio,0,0);maskCtx.fillStyle="#000";maskCtx.fillRect(0,0,r.width,r.height);var o=selected(),empty=byId("selected-mask-empty"),name=byId("selected-mask-name");name.textContent=o?(o.name||"Unnamed mask"):"No mask selected";empty.classList.toggle("hidden",Boolean(o));if(o&&state.review){var v=selectedMaskView();maskCtx.drawImage(tint(o._canvas,"#ffffff"),v.x,v.y,v.width,v.height)}updateSelectedMaskCursor()}
function updateSelectedMaskCursor(){var active=state.tool==="positive"||state.tool==="negative"||state.tool==="box"||state.tool==="brush"||state.tool==="erase",visible=Boolean(active&&state.cursorPoint&&state.review&&selected()&&!state.panning&&state.stage===0),v=visible&&selectedMaskView();selectedMaskCursor.hidden=!visible;if(!visible||!v)return;selectedMaskCursor.className="selected-mask-cursor cursor-"+state.tool;selectedMaskCursor.style.left=(v.x+state.cursorPoint[0]*v.scale)+"px";selectedMaskCursor.style.top=(v.y+state.cursorPoint[1]*v.scale)+"px";if(state.tool==="brush"||state.tool==="erase"){var imageDiameter=state.brushSize/Math.max(.01,state.view?state.view.scale:v.scale),diameter=Math.max(4,imageDiameter*v.scale);selectedMaskCursor.style.width=diameter+"px";selectedMaskCursor.style.height=diameter+"px"}else{selectedMaskCursor.style.width="18px";selectedMaskCursor.style.height="18px"}}
function updatePaintCursor(){var visible=Boolean(state.cursorPoint&&state.view&&!state.panning&&(state.tool==="brush"||state.tool==="erase"));paintCursor.hidden=!visible;canvas.classList.toggle("has-paint-cursor",visible);if(visible){var v=state.view;paintCursor.style.left=(v.x+state.cursorPoint[0]*v.scale)+"px";paintCursor.style.top=(v.y+state.cursorPoint[1]*v.scale)+"px";paintCursor.style.width=state.brushSize+"px";paintCursor.style.height=state.brushSize+"px";paintCursor.classList.toggle("erase",state.tool==="erase")}updateSelectedMaskCursor()}
async function predict(){var o=selected();if(!o)return;setSystem("SAM3 running","running");try{var payload={points:state.points,labels:state.labels,box:state.box,mask_b64:o._canvas.toDataURL("image/png").split(",")[1]},r=await api("/api/demo/cases/"+encodeURIComponent(state.caseId)+"/predict",json("POST",payload));if(r.masks&&r.masks.length){state.candidates=[];for(var i=0;i<r.masks.length;i++)state.candidates.push(await maskFrom(r.masks[i],state.review.image.width,state.review.image.height));state.candidateIndex=r.best_idx||0;o._canvas=state.candidates[state.candidateIndex];renderCandidates();draw()}setSystem("Ready","ready")}catch(e){setSystem("Runtime unavailable","failed");toast(e.message,true)}}
canvas.onpointerdown=function(e){if((state.spacePressed||e.button===1)&&state.review){e.preventDefault();state.panning=true;state.panStart={x:e.clientX,y:e.clientY,panX:state.panX,panY:state.panY};canvas.classList.add("panning");canvas.setPointerCapture(e.pointerId);updatePaintCursor();return}var p=point(e),o=selected();if(!p)return;state.cursorPoint=p;updatePaintCursor();if(state.tool==="positive"||state.tool==="negative"){state.points.push(p);state.labels.push(state.tool==="positive"?1:0);updatePromptCounts();draw();predict()}else if(state.tool==="box"){state.boxStart=p;state.box=[p[0],p[1],p[0],p[1]];state.drawing=true;canvas.setPointerCapture(e.pointerId);draw()}else if(state.tool==="brush"||state.tool==="erase"){if(!o){toast("Select a mask first",true);return}pushUndo();state.drawing=true;paint(p);canvas.setPointerCapture(e.pointerId)}else if(state.tool==="select"){for(var i=state.objects.length-1;i>=0;i--){var item=state.objects[i];if(item._visible===false)continue;var px=item._canvas.getContext("2d").getImageData(Math.floor(p[0]),Math.floor(p[1]),1,1).data;if(px[3]>20||px[0]>127){selectObject(item.id);break}}}};
canvas.onpointermove=function(e){if(state.panning&&state.panStart){state.panX=state.panStart.panX+e.clientX-state.panStart.x;state.panY=state.panStart.panY+e.clientY-state.panStart.y;draw();return}var p=point(e);state.cursorPoint=p;updatePaintCursor();byId("cursorMeta").textContent=p?("x "+Math.round(p[0])+" · y "+Math.round(p[1])):"x — · y —";if(!p)return;if(state.drawing&&state.tool==="box"){state.box=[Math.min(state.boxStart[0],p[0]),Math.min(state.boxStart[1],p[1]),Math.max(state.boxStart[0],p[0]),Math.max(state.boxStart[1],p[1])];draw()}else if(state.drawing&&(state.tool==="brush"||state.tool==="erase"))paint(p)};
canvas.onpointerup=function(e){if(state.panning){state.panning=false;state.panStart=null;canvas.classList.remove("panning");updatePaintCursor();return}if(state.tool==="box"&&state.boxStart){var p=point(e);if(p)state.box=[Math.min(state.boxStart[0],p[0]),Math.min(state.boxStart[1],p[1]),Math.max(state.boxStart[0],p[0]),Math.max(state.boxStart[1],p[1])];if(state.box&&(state.box[2]-state.box[0]<2||state.box[3]-state.box[1]<2))state.box=null;state.boxStart=null;state.drawing=false;updatePromptCounts();draw();if(state.box)predict()}state.drawing=false};
canvas.onpointercancel=function(){state.drawing=false;state.boxStart=null;state.panning=false;state.panStart=null;state.cursorPoint=null;canvas.classList.remove("panning");updatePaintCursor()};
canvas.onpointerleave=function(){state.cursorPoint=null;updatePaintCursor()};
canvas.onwheel=function(e){if(!state.review)return;e.preventDefault();var r=canvas.getBoundingClientRect(),factor=e.deltaY<0?1.16:1/1.16;setCanvasZoom(state.zoom*factor,e.clientX-r.left,e.clientY-r.top)};
function cloneCanvas(source){var c=document.createElement("canvas");c.width=source.width;c.height=source.height;c.getContext("2d").drawImage(source,0,0);return c}
function pushUndo(){var o=selected();if(!o)return;state.undo.push({id:o.id,canvas:cloneCanvas(o._canvas)});if(state.undo.length>50)state.undo.shift();state.redo=[];syncHistory()}
function syncHistory(){byId("undo-paint").disabled=!state.undo.length;byId("redo-paint").disabled=!state.redo.length}
function undo(){var o=selected(),x=state.undo.pop();if(!o||!x)return;state.redo.push({id:o.id,canvas:cloneCanvas(o._canvas)});o._canvas=x.canvas;syncHistory();draw()}
function redo(){var o=selected(),x=state.redo.pop();if(!o||!x)return;state.undo.push({id:o.id,canvas:cloneCanvas(o._canvas)});o._canvas=x.canvas;syncHistory();draw()}
function paint(p){var o=selected();if(!o||!p)return;var x=o._canvas.getContext("2d"),radius=state.brushSize/(2*Math.max(.01,state.view.scale));x.save();x.globalCompositeOperation=state.tool==="erase"?"destination-out":"source-over";x.fillStyle="#fff";x.beginPath();x.arc(p[0],p[1],radius,0,Math.PI*2);x.fill();x.restore();draw()}
async function saveReview(){if(!state.review)return;try{var objects=state.objects.map(function(o){return{id:o.id,name:o.name.trim(),source:"human-review",mask_b64:o._canvas.toDataURL("image/png").split(",")[1]}});state.review=await api("/api/demo/cases/"+encodeURIComponent(state.caseId)+"/review",json("PUT",{objects:objects}));toast("Saved review revision "+state.review.revision);await openCase(state.caseId,true)}catch(e){toast(e.message,true)}}
async function addText(){var prompt=byId("text-prompt").value.trim();if(!prompt||!state.review)return;setSystem("SAM3 running","running");try{var r=await api("/api/demo/cases/"+encodeURIComponent(state.caseId)+"/predict",json("POST",{prompt:prompt}));if(!r.masks.length)throw new Error("SAM3 returned no masks");var id="obj-"+Date.now();state.objects.push({id:id,name:prompt,source:"sam3-text",_visible:true,_canvas:await maskFrom(r.masks[r.best_idx||0],state.review.image.width,state.review.image.height)});state.selectedId=id;byId("text-prompt").value="";renderObjects();draw();setSystem("Ready","ready")}catch(e){setSystem("Runtime unavailable","failed");toast(e.message,true)}}
function addEmpty(){if(!state.review)return;var c=document.createElement("canvas");c.width=state.review.image.width;c.height=state.review.image.height;var id="obj-"+Date.now();state.objects.push({id:id,name:"new object",source:"sam3-prompt",_visible:true,_canvas:c});state.selectedId=id;state.tool="positive";renderObjects();setTool("positive")}
function setTool(tool){state.tool=tool;state.cursorPoint=null;document.querySelectorAll(".tool").forEach(function(b){b.classList.toggle("active",b.dataset.tool===tool)});byId("brush-size-control").hidden=tool!=="brush"&&tool!=="erase";["select","positive","negative","box","brush","erase"].forEach(function(name){canvas.classList.toggle("tool-"+name,name===tool)});updatePaintCursor();requestAnimationFrame(draw)}
function detailsHtml(s){var files=s&&s.files||[],methods=s&&s.methods||[];return'<h2>Stage '+state.stage+' · '+esc(stageNames[state.stage])+'</h2><p>'+(s&&s.ready?'Outputs are available for this case.':'No output has been generated for this case yet.')+'</p><strong>'+esc(s&&s.summary||"Pending")+'</strong>'+(methods.length?'<p>Methods: '+methods.map(function(m){return esc(m.method)}).join(", ")+'</p>':'')+(files.length?'<ul>'+files.slice(0,25).map(function(f){return'<li><a target="_blank" rel="noopener" href="'+esc(f.url)+'">'+esc(f.name)+'</a></li>'}).join("")+'</ul>':"")}
function stageMethodFile(stage,method,suffix){var group=(stage&&stage.methods||[]).find(function(m){return m.method===method});return group&&(group.files||[]).find(function(f){return f.name.endsWith(suffix)})}
function clearStageViewers(){if(state.cleanup){state.cleanup();state.cleanup=null}if(state.overlayCleanup){state.overlayCleanup();state.overlayCleanup=null}document.querySelectorAll(".ccm-pcd-viewer,.ccm-mesh-viewer").forEach(function(host){if(host._ccmCleanup){host._ccmCleanup();host._ccmCleanup=null}});if(state.ccmImageCleanup){state.ccmImageCleanup();state.ccmImageCleanup=null}if(state.graphCleanup){state.graphCleanup();state.graphCleanup=null}}
function syncStage1ResultFlags(){var stage=state.caseData&&state.caseData.stages&&state.caseData.stages[1],items=ccmItemsFromStage(stage);state.stage1Results.ccm=items.some(function(item){return Boolean(item&&(item.overlay_url||item.ccm_url||item.cropped_url))});state.stage1Results.mesh=items.some(function(item){return Boolean(item&&item.mesh_url)})}
function renderResultStage(){clearStageViewers();var root=byId("result-stage");root.replaceChildren();root.classList.remove("hidden");if(state.stage===1){syncStage1ResultFlags();renderCCMStage(root);return}if(state.stage===2){renderConstructSceneStage(root);return}root.classList.add("hidden")}
function setupStageOverlay(){var opacity=byId("stage-overlay-opacity"),canvas=byId("stage-rgb-overlay");if(!opacity||!canvas)return;opacity.oninput=function(){state.depthOverlayOpacity=Number(opacity.value)/100;drawStageOverlay("stage-rgb-overlay")};var observer=new ResizeObserver(function(){drawStageOverlay("stage-rgb-overlay")});observer.observe(canvas);state.overlayCleanup=function(){observer.disconnect()};requestAnimationFrame(function(){drawStageOverlay("stage-rgb-overlay")})}
function ccmItemsFromStage(stage){var current=(stage&&stage.items)||[],legacyMeshStage=(state.caseData&&state.caseData.stages||[]).find(function(candidate){return candidate&&candidate.key==="inference_mesh"}),files=[].concat(stage&&stage.files||[],legacyMeshStage&&legacyMeshStage.files||[]),byName=new Map(files.map(function(file){return[file.name,file]})),overlays=files.filter(function(file){return /(?:^|\/)canonical_pcd_(\d+)_overlay\.ply$/i.test(file.name)}),meshes=files.filter(function(file){return /(?:^|\/)(\d+)\.(?:glb|gltf)$/i.test(file.name)&&!/_overlay\.(?:glb|gltf)$/i.test(file.name)}),ccm=byName.get("CCM/canonical_coord_map_restored.png")||files.find(function(file){return /(?:^|\/)canonical_coord_map_restored\.png$/i.test(file.name)}),cropped=byName.get("CCM/rgb_ccm_cropped.png")||files.find(function(file){return /(?:^|\/)rgb_ccm_cropped\.png$/i.test(file.name)}),masks=state.caseData&&state.caseData.masks||[],indices=new Set(masks.map(function(_,index){return index}));current.forEach(function(item){indices.add(item.index)});overlays.concat(meshes).forEach(function(file){indices.add(Number(file.name.match(/(\d+)(?:_overlay)?\.(?:ply|glb|gltf)$/i)[1]))});var byIndex=new Map(current.map(function(item){return[item.index,item]})),overlayByIndex=new Map(overlays.map(function(file){return[Number(file.name.match(/canonical_pcd_(\d+)_overlay\.ply$/i)[1]),file]})),meshByIndex=new Map(meshes.map(function(file){return[Number(file.name.match(/(\d+)\.(?:glb|gltf)$/i)[1]),file]})),imageWidth=state.review&&state.review.image&&state.review.image.width||518,imageHeight=state.review&&state.review.image&&state.review.image.height||518;return Array.from(indices).sort(function(a,b){return a-b}).map(function(index){var existing=byIndex.get(index)||{},mask=masks[index],cropWidth=existing.cropped_size&&existing.cropped_size.width||imageWidth,cropHeight=existing.cropped_size&&existing.cropped_size.height||imageHeight;return Object.assign({},existing,{index:index,label:existing.label||mask&&mask.name||"object_"+String(index).padStart(3,"0"),cropped_url:existing.cropped_url||cropped&&cropped.url,overlay_url:existing.overlay_url||overlayByIndex.get(index)&&overlayByIndex.get(index).url,mesh_url:existing.mesh_url||meshByIndex.get(index)&&meshByIndex.get(index).url,cropped_crops:existing.cropped_crops||cropped?existing.cropped_crops||{rgb:{x:0,y:index*cropHeight,width:cropWidth,height:cropHeight},mask:{x:cropWidth,y:index*cropHeight,width:cropWidth,height:cropHeight},ccm:{x:cropWidth*2,y:index*cropHeight,width:cropWidth,height:cropHeight}}:null})})}
function renderCCMStage(root){var stage=state.caseData&&state.caseData.stages&&state.caseData.stages[1],items=ccmItemsFromStage(stage),header=document.createElement("div");header.className="ccm-stage-header";var copy=document.createElement("div");copy.innerHTML='<span class="eyebrow">STAGE 1 · INFERENCE CCM + MESH</span><h1>Cropped RGB · Mask · CCM · Canonical PCD · Mesh</h1><p>Existing CCM and mesh outputs are displayed automatically. Use the sidebar actions only to rerun inference.</p>';header.append(copy);root.append(header);var columns=document.createElement("div");columns.className="ccm-column-headings";columns.innerHTML='<span>Object</span><span>Cropped RGB</span><span>Cropped Mask</span><span>Cropped CCM</span><span>Canonical PCD overlay</span><span>Mesh GLB</span>';root.append(columns);var rows=document.createElement("div");rows.className="ccm-example-list";root.append(rows);if(!items.length){rows.innerHTML='<div class="viewer-empty ccm-empty"><strong>No object examples available</strong><span>CCM and mesh results will appear here after inference.</span></div>';return}items.forEach(function(item,index){var row=document.createElement("article");row.className="ccm-example-row";row.dataset.index=String(item.index);row.innerHTML='<div class="ccm-row-label"><strong>'+esc(item.label||("object_"+String(item.index).padStart(3,"0")))+'</strong><span>Object '+String(item.index).padStart(3,"0")+'</span></div><div class="ccm-image-media" data-empty-label="Cropped RGB unavailable"><canvas class="ccm-cropped-canvas" data-part="rgb" aria-label="Cropped RGB"></canvas></div><div class="ccm-image-media" data-empty-label="Cropped mask unavailable"><canvas class="ccm-cropped-canvas" data-part="mask" aria-label="Cropped mask"></canvas></div><div class="ccm-image-media" data-empty-label="Cropped CCM unavailable"><canvas class="ccm-cropped-canvas" data-part="ccm" aria-label="Cropped CCM"></canvas></div><div class="ccm-pcd-viewer"></div><div class="ccm-mesh-viewer"></div>';rows.append(row);drawCCMCroppedRow(row,item);setupCCMRowObserver(row,item,index===0)})}
function drawCCMCroppedRow(row,item){var media=row.querySelectorAll(".ccm-image-media");if(!item||!item.cropped_url){media.forEach(function(host){host.classList.add("ccm-missing");host.innerHTML='<span>'+esc(host.dataset.emptyLabel||"Not available")+'</span>'});return}var canvases=row.querySelectorAll(".ccm-cropped-canvas"),image=new Image();image.onload=function(){var crops=item.cropped_crops||{},parts=["rgb","mask","ccm"];canvases.forEach(function(canvas,index){var host=canvas.closest(".ccm-image-media");if(index===2&&!state.stage1Results.ccm){host.classList.add("ccm-missing");canvas.hidden=true;return}host.classList.remove("ccm-missing");canvas.hidden=false;var crop=crops[parts[index]]||{x:index*image.naturalWidth/3,y:0,width:image.naturalWidth/3,height:image.naturalHeight};canvas.width=Math.max(1,Math.round(crop.width));canvas.height=Math.max(1,Math.round(crop.height));canvas.getContext("2d").drawImage(image,crop.x,crop.y,crop.width,crop.height,0,0,canvas.width,canvas.height)})};image.onerror=function(){media.forEach(function(host){host.classList.add("ccm-missing");host.innerHTML='<span>rgb_ccm_cropped.png unavailable</span>'})};image.src=item.cropped_url}
function renderCCMItem(item){if(!item)return;drawCCMCroppedRow(document.querySelector('.ccm-example-row[data-index="'+String(item.index)+'"]'),item);loadCCMPointCloud(item)}
function setupCCMRowObserver(row,item,loadImmediately){var pcdViewer=row.querySelector(".ccm-pcd-viewer"),meshViewer=row.querySelector(".ccm-mesh-viewer"),loaded=false,load=function(){if(loaded)return;loaded=true;if(state.stage1Results.ccm){if(item.overlay_url)loadCCMPointCloud(item,pcdViewer);else pcdViewer.innerHTML='<div class="viewer-empty"><strong>PCD unavailable</strong><span>Run Inference CCM to generate this result.</span></div>'}else pcdViewer.innerHTML='<div class="viewer-empty"><strong>CCM not loaded</strong><span>Use Inference CCM in the sidebar.</span></div>';if(state.stage1Results.mesh){if(item.mesh_url)loadCCMMesh(item,meshViewer);else meshViewer.innerHTML='<div class="viewer-empty"><strong>Mesh unavailable</strong><span>Run Inference Mesh to generate this result.</span></div>'}else meshViewer.innerHTML='<div class="viewer-empty"><strong>Mesh not loaded</strong><span>Use Inference Mesh in the sidebar.</span></div>'},release=function(){[pcdViewer,meshViewer].forEach(function(host){if(host._ccmCleanup){host._ccmCleanup();host._ccmCleanup=null}});loaded=false};row._ccmLoad=function(){loaded=false;load()};if(loadImmediately)load();if("IntersectionObserver" in window){var observer=new IntersectionObserver(function(entries){entries.forEach(function(entry){if(entry.isIntersecting)load();else release()})},{root:byId("result-stage"),rootMargin:"120px"});observer.observe(row)}else load()}
function refreshCCMStageResults(kind){if(state.stage!==1)return;state.stage1Results[kind]=true;document.querySelectorAll(".ccm-example-row").forEach(function(row){var itemIndex=Number(row.dataset.index),stage=state.caseData&&state.caseData.stages&&state.caseData.stages[1],item=ccmItemsFromStage(stage).find(function(value){return value.index===itemIndex});if(!item)return;if(kind==="ccm"){drawCCMCroppedRow(row,item);var pcd=row.querySelector(".ccm-pcd-viewer");if(pcd&&pcd._ccmCleanup){pcd._ccmCleanup();pcd._ccmCleanup=null}if(pcd)pcd.innerHTML=""}if(kind==="mesh"){var mesh=row.querySelector(".ccm-mesh-viewer");if(mesh&&mesh._ccmCleanup){mesh._ccmCleanup();mesh._ccmCleanup=null}if(mesh)mesh.innerHTML=""}if(row._ccmLoad)row._ccmLoad()})}
function registerCCMViewer(host){state.ccmViewers=state.ccmViewers.filter(function(item){return item!==host&&item._ccmCleanup});while(state.ccmViewers.length>=6){var oldest=state.ccmViewers.shift();if(oldest&&oldest._ccmCleanup){oldest._ccmCleanup();oldest._ccmCleanup=null;oldest.innerHTML='<div class="viewer-empty"><strong>Viewer released</strong><span>Reload the stage to open this result again.</span></div>'}}state.ccmViewers.push(host)}
async function loadCCMPointCloud(item,host){host=host||byId("ccm-pcd-viewer");if(!host||!item||!item.overlay_url)return;if(host._ccmCleanup){host._ccmCleanup();host._ccmCleanup=null}host.innerHTML='<div class="viewer-loading"><span class="spinner"></span><strong>Loading canonical overlay…</strong></div>';try{var modules=await Promise.all([import('../../../vendor/three/three.module.js'),import('../../../vendor/three/PLYLoader.js'),import('../../../vendor/three/OrbitControls.js')]),THREE=modules[0],PLYLoader=modules[1].PLYLoader,OrbitControls=modules[2].OrbitControls,geometry=await new PLYLoader().loadAsync(item.overlay_url);if(!document.body.contains(host)){geometry.dispose();return}geometry.computeBoundingBox();geometry.computeBoundingSphere();var position=geometry.getAttribute("position"),width=Math.max(320,host.clientWidth),height=Math.max(220,host.clientHeight),renderer=new THREE.WebGLRenderer({antialias:true,alpha:false});renderer.setPixelRatio(Math.min(window.devicePixelRatio||1,2));renderer.setSize(width,height,false);renderer.setClearColor(0x080a0d,1);renderer.outputColorSpace=THREE.SRGBColorSpace;host.replaceChildren(renderer.domElement);var scene=new THREE.Scene(),hasColors=Boolean(geometry.getAttribute("color")),radius=Math.max(geometry.boundingSphere&&geometry.boundingSphere.radius||1,.001),material=new THREE.PointsMaterial({size:Math.max(.002,radius/420),vertexColors:hasColors,color:hasColors?0xffffff:0xd7ff4f,sizeAttenuation:true}),points=new THREE.Points(geometry,material);points.rotation.x=-Math.PI/2;points.updateMatrixWorld(true);scene.add(points);var box=new THREE.Box3().setFromObject(points),center=box.getCenter(new THREE.Vector3()),rotatedSize=box.getSize(new THREE.Vector3()),rotatedRadius=Math.max(rotatedSize.length()/2,.001),camera=new THREE.PerspectiveCamera(60,width/height,Math.max(rotatedRadius/1000,.0001),rotatedRadius*30),controls=new OrbitControls(camera,renderer.domElement);controls.target.copy(center);camera.position.copy(center).add(new THREE.Vector3(0,rotatedRadius*.2,rotatedRadius*1.8));camera.lookAt(center);controls.update();var alive=true,observer=new ResizeObserver(function(){if(!alive)return;var w=Math.max(1,host.clientWidth),h=Math.max(220,host.clientHeight);camera.aspect=w/h;camera.updateProjectionMatrix();renderer.setSize(w,h,false)});observer.observe(host);(function loop(){if(!alive)return;controls.update();renderer.render(scene,camera);requestAnimationFrame(loop)})();host._ccmCleanup=function(){alive=false;observer.disconnect();controls.dispose();geometry.dispose();material.dispose();renderer.dispose();if(renderer.domElement.parentNode)renderer.domElement.remove()}}catch(e){host.innerHTML='<div class="viewer-empty error"><strong>Unable to load canonical PCD overlay</strong><span>'+esc(e.message)+'</span><a href="'+esc(item.overlay_url)+'" target="_blank" rel="noopener">Open source file</a></div>'}}
async function loadCCMMesh(item,host){if(!host||!item||!item.mesh_url)return;if(host._ccmCleanup){host._ccmCleanup();host._ccmCleanup=null}host.innerHTML='<div class="viewer-loading"><span class="spinner"></span><strong>Loading mesh…</strong></div>';try{var modules=await Promise.all([import('../../../vendor/three/three.module.js'),import('../../../vendor/three/GLTFLoader.js'),import('../../../vendor/three/OrbitControls.js')]),THREE=modules[0],GLTFLoader=modules[1].GLTFLoader,OrbitControls=modules[2].OrbitControls,model=await new GLTFLoader().loadAsync(item.mesh_url);if(!document.body.contains(host))return;var sourceMaterials=[];model.scene.traverse(function(object){if(!object.isMesh||!object.material)return;var originals=Array.isArray(object.material)?object.material:[object.material],basics=originals.map(function(material){sourceMaterials.push(material);var basic=new THREE.MeshBasicMaterial({color:material.color?material.color.clone():new THREE.Color(0xffffff),map:material.map||null,alphaMap:material.alphaMap||null,transparent:Boolean(material.transparent),opacity:material.opacity===undefined?1:material.opacity,alphaTest:material.alphaTest||0,side:material.side,vertexColors:Boolean(material.vertexColors)});basic.name=material.name||"texture-only";return basic});object.material=Array.isArray(object.material)?basics:basics[0]});var width=Math.max(260,host.clientWidth),height=Math.max(220,host.clientHeight),renderer=new THREE.WebGLRenderer({antialias:true,alpha:true});renderer.setPixelRatio(Math.min(window.devicePixelRatio||1,2));renderer.setSize(width,height,false);renderer.outputColorSpace=THREE.SRGBColorSpace;renderer.setClearColor(0x080a0d,1);host.replaceChildren(renderer.domElement);var scene=new THREE.Scene();scene.add(model.scene);var box=new THREE.Box3().setFromObject(model.scene),center=box.getCenter(new THREE.Vector3()),size=box.getSize(new THREE.Vector3()),radius=Math.max(size.length()/2,.001),camera=new THREE.PerspectiveCamera(60,width/height,Math.max(radius/1000,.0001),radius*30),controls=new OrbitControls(camera,renderer.domElement);controls.target.copy(center);camera.position.copy(center).add(new THREE.Vector3(0,radius*.2,radius*1.8));camera.lookAt(center);controls.update();var alive=true,observer=new ResizeObserver(function(){if(!alive)return;var w=Math.max(1,host.clientWidth),h=Math.max(220,host.clientHeight);camera.aspect=w/h;camera.updateProjectionMatrix();renderer.setSize(w,h,false)});observer.observe(host);(function loop(){if(!alive)return;controls.update();renderer.render(scene,camera);requestAnimationFrame(loop)})();host._ccmCleanup=function(){alive=false;observer.disconnect();controls.dispose();model.scene.traverse(function(object){if(object.geometry)object.geometry.dispose();if(object.material){var materials=Array.isArray(object.material)?object.material:[object.material];materials.forEach(function(material){material.dispose()})}});sourceMaterials.forEach(function(material){Object.values(material).forEach(function(value){if(value&&value.isTexture)value.dispose()});material.dispose()});renderer.dispose();if(renderer.domElement.parentNode)renderer.domElement.remove()}}catch(e){host.innerHTML='<div class="viewer-empty error"><strong>Unable to load mesh GLB</strong><span>'+esc(e.message)+'</span><a href="'+esc(item.mesh_url)+'" target="_blank" rel="noopener">Open source file</a></div>'}}
function setupConstructOptionsPanel(){var panel=byId("construct-options-panel");if(!panel)return;panel.hidden=state.stage!==2;var depth=panel.querySelector('[data-construct-option="estimation_depth"]'),floor=panel.querySelector('[data-construct-option="estimate_floor"]');panel.querySelectorAll("input[data-construct-option]").forEach(function(input){var key=input.dataset.constructOption;input.checked=Boolean(state.constructOptions[key]);input.disabled=Boolean(state.pipelineJob&&/queued|running|cancelling/.test(state.pipelineJob.status));input.onchange=function(){state.constructOptions[key]=input.checked;if(key==="estimation_depth"&&input.checked){state.constructOptions.estimate_floor=true;floor.checked=true}floor.disabled=Boolean(state.constructOptions.estimation_depth)||Boolean(state.pipelineJob&&/queued|running|cancelling/.test(state.pipelineJob.status))}});if(depth&&floor)floor.disabled=Boolean(depth.checked)||Boolean(state.pipelineJob&&/queued|running|cancelling/.test(state.pipelineJob.status))}
function renderConstructSceneStage(root){setupConstructOptionsPanel();var grid=document.createElement("div");grid.className="construct-scene-grid";grid.innerHTML='<article class="result-card scene-graph-card construct-graph-card"><div class="result-card-heading"><h2>Scene graph</h2><div class="graph-heading-actions"><span>select a node to edit</span><div class="graph-zoom-controls" role="group" aria-label="Scene graph zoom"><button id="graph-zoom-out" type="button" aria-label="Zoom out">−</button><button id="graph-zoom-reset" type="button" aria-label="Reset zoom">100%</button><button id="graph-zoom-in" type="button" aria-label="Zoom in">＋</button></div><button id="reset-scene-graph" type="button" disabled>Reset</button><button id="save-scene-graph" type="button" disabled>Save</button></div></div><div id="stage-graph" class="scene-graph"><div class="viewer-loading"><span class="spinner"></span><strong>Loading scene graph…</strong></div></div><div id="scene-graph-editor" class="scene-graph-editor"><span>Select an object node to edit its support relationship.</span></div></article><article class="result-card post-stage-overlay-card construct-overlay-card"><div class="result-card-heading"><h2>RGB + mask overlay</h2><label class="stage-overlay-control">Overlay <input id="stage-overlay-opacity" type="range" min="0" max="100" value="'+Math.round(state.depthOverlayOpacity*100)+'"></label></div><div class="post-stage-overlay-media"><canvas id="stage-rgb-overlay" aria-label="RGB image with segmentation mask overlays"></canvas></div></article><article class="result-card construct-viewer-card"><div class="result-card-heading"><h2>Interactive 3D scene</h2><div class="environment-heading-actions"><span>drag to orbit · scroll to zoom</span><label class="stage-overlay-control environment-rotation-control">Environment <input id="environment-rotation" type="range" min="-180" max="180" step="1" value="'+Math.round(state.environmentRotationYDegrees)+'" aria-label="Environment panorama rotation in degrees"><output id="environment-rotation-value">'+Math.round(state.environmentRotationYDegrees)+'°</output></label></div></div><div id="stage-scene-viewer" class="result-viewer construct-scene-viewer"><div class="viewer-loading"><span class="spinner"></span><strong>Loading scene…</strong></div></div></article>';root.append(grid);setupStageOverlay();setupEnvironmentRotationControl();byId("graph-zoom-out").onclick=function(){setSceneGraphZoom(state.graphZoom-.1)};byId("graph-zoom-reset").onclick=function(){setSceneGraphZoom(1)};byId("graph-zoom-in").onclick=function(){setSceneGraphZoom(state.graphZoom+.1)};byId("reset-scene-graph").onclick=resetSceneGraph;byId("save-scene-graph").onclick=saveSceneGraph;updateSceneGraphZoom();renderSceneGraph();render3d()}
function loadEnvironmentRotation(caseId){var value=stage2ViewerDefaults.panoramaRotationYDegrees;if(caseId){try{var saved=JSON.parse(localStorage.getItem(environmentRotationStorageKey)||"{}");var candidate=Number(saved[caseId]);if(Number.isFinite(candidate))value=Math.max(-180,Math.min(180,candidate))}catch(_){}}state.environmentRotationYDegrees=value}
function saveEnvironmentRotation(){if(!state.caseId)return;try{var saved=JSON.parse(localStorage.getItem(environmentRotationStorageKey)||"{}");if(!saved||typeof saved!=="object")saved={};saved[state.caseId]=state.environmentRotationYDegrees;localStorage.setItem(environmentRotationStorageKey,JSON.stringify(saved))}catch(_){} }
function applyEnvironmentRotation(){var scene=state.stage2EnvironmentScene;if(scene&&scene.backgroundRotation)scene.backgroundRotation.y=state.environmentRotationYDegrees*Math.PI/180}
function setupEnvironmentRotationControl(){var input=byId("environment-rotation"),output=byId("environment-rotation-value");if(!input)return;var available=Boolean(state.caseData&&state.caseData.environment_url);input.disabled=!available;input.title=available?"Rotate the environment panorama around the scene":"Environment panorama is not available for this case";input.value=String(Math.round(state.environmentRotationYDegrees));function update(){state.environmentRotationYDegrees=Number(input.value);if(output)output.value=String(Math.round(state.environmentRotationYDegrees))+"°";saveEnvironmentRotation();applyEnvironmentRotation()}input.oninput=update;update()}
function renderDepthStage(root){var stage=state.caseData&&state.caseData.stages&&state.caseData.stages[1],methods=(stage&&stage.methods||[]).filter(function(m){return stageMethodFile(stage,m.method,"camera_pts.ply")});var header=document.createElement("div");header.className="depth-stage-header";var copy=document.createElement("div");copy.innerHTML='<span class="eyebrow">STAGE 1 · ESTIMATE DEPTH</span><h1>RGB &amp; Camera Point Cloud</h1><p>Colored camera-space points generated by the selected depth method.</p>';header.append(copy);var control=document.createElement("label");control.className="method-select-label";control.textContent="Depth method";var select=document.createElement("select");select.id="depth-method";select.setAttribute("aria-label","Depth method");methods.forEach(function(m){select.append(new Option(m.method,m.method))});control.append(select);header.append(control);root.append(header);var grid=document.createElement("div");grid.className="depth-stage-grid";var rgb=document.createElement("article");rgb.className="result-card depth-card";rgb.innerHTML='<div class="result-card-heading"><h2>RGB + mask overlay</h2><label class="stage-overlay-control">Overlay <input id="depth-overlay-opacity" type="range" min="0" max="100" value="'+Math.round(state.depthOverlayOpacity*100)+'"></label></div><div class="depth-media"><canvas id="depth-rgb-overlay" aria-label="RGB image with segmentation mask overlays"></canvas></div>';var cloud=document.createElement("article");cloud.className="result-card depth-card";cloud.innerHTML='<div class="result-card-heading"><h2>Camera point cloud</h2><span id="depth-method-meta"></span></div><div id="depth-ply-viewer" class="depth-media point-cloud-viewer"><div class="viewer-loading"><span class="spinner"></span><strong>Loading point cloud…</strong></div></div>';grid.append(rgb,cloud);root.append(grid);var opacity=byId("depth-overlay-opacity");opacity.oninput=function(){state.depthOverlayOpacity=Number(opacity.value)/100;drawDepthOverlay()};var observer=new ResizeObserver(drawDepthOverlay);observer.observe(byId("depth-rgb-overlay"));state.overlayCleanup=function(){observer.disconnect()};requestAnimationFrame(drawDepthOverlay);if(!methods.length){select.disabled=true;byId("depth-ply-viewer").innerHTML='<div class="viewer-empty"><strong>camera_pts.ply not available</strong><span>This case has no point-cloud result under depth/&lt;method&gt;.</span></div>';return}select.onchange=function(){loadDepthPointCloud(stage,select.value)};loadDepthPointCloud(stage,select.value)}
function drawDepthOverlay(){drawStageOverlay("depth-rgb-overlay")}
function drawStageOverlay(canvasId){var c=byId(canvasId);if(!c||!state.image||!state.review)return;var x=c.getContext("2d"),r=c.getBoundingClientRect(),ratio=window.devicePixelRatio||1,w=Math.max(1,Math.round(r.width*ratio)),h=Math.max(1,Math.round(r.height*ratio));if(c.width!==w||c.height!==h){c.width=w;c.height=h}x.setTransform(ratio,0,0,ratio,0,0);x.fillStyle="#080a0d";x.fillRect(0,0,r.width,r.height);var margin=12,scale=Math.min(Math.max(1,r.width-margin*2)/state.review.image.width,Math.max(1,r.height-margin*2)/state.review.image.height),dw=state.review.image.width*scale,dh=state.review.image.height*scale,dx=(r.width-dw)/2,dy=(r.height-dh)/2;x.drawImage(state.image,dx,dy,dw,dh);state.objects.forEach(function(o,i){if(o._visible===false)return;x.save();x.globalAlpha=state.depthOverlayOpacity;x.drawImage(tint(o._canvas,palette[i%palette.length]),dx,dy,dw,dh);x.restore()})}
function containedImageRect(width,height,imageWidth,imageHeight,margin){var availableWidth=Math.max(1,width-margin*2),availableHeight=Math.max(1,height-margin*2),scale=Math.min(availableWidth/imageWidth,availableHeight/imageHeight),drawWidth=imageWidth*scale,drawHeight=imageHeight*scale;return{x:(width-drawWidth)/2,y:(height-drawHeight)/2,width:drawWidth,height:drawHeight}}
function parseNpyMatrix3(buffer){var bytes=new Uint8Array(buffer),view=new DataView(buffer);if(bytes.length<12||bytes[0]!==147||String.fromCharCode.apply(null,bytes.slice(1,6))!=="NUMPY")throw new Error("intrinsics.npy has an invalid NPY header");var major=bytes[6],headerLength,headerStart;if(major===1){headerLength=view.getUint16(8,true);headerStart=10}else if(major===2||major===3){headerLength=view.getUint32(8,true);headerStart=12}else throw new Error("intrinsics.npy uses an unsupported NPY version");var headerEnd=headerStart+headerLength;if(headerEnd>bytes.length)throw new Error("intrinsics.npy is truncated");var header=new TextDecoder("latin1").decode(bytes.slice(headerStart,headerEnd)),descrMatch=header.match(/["']descr["']\s*:\s*["']([^"']+)["']/),shapeMatch=header.match(/["']shape["']\s*:\s*\(([^)]*)\)/),fortranMatch=header.match(/["']fortran_order["']\s*:\s*(True|False)/);if(!descrMatch||!shapeMatch||!fortranMatch)throw new Error("intrinsics.npy header is incomplete");if(fortranMatch[1]!=="False")throw new Error("Fortran-order intrinsics.npy is unsupported");var shape=shapeMatch[1].split(",").map(function(value){return Number(value.trim())}).filter(function(value){return Number.isFinite(value)});if(shape.length!==2||shape[0]!==3||shape[1]!==3)throw new Error("intrinsics.npy must contain a 3×3 matrix");var descr=descrMatch[1],typeMatch=descr.match(/^([<>=|])f(4|8)$/);if(!typeMatch||typeMatch[1]===">")throw new Error("intrinsics.npy must use little-endian float32 or float64 values");var itemBytes=Number(typeMatch[2]),dataEnd=headerEnd+9*itemBytes;if(dataEnd>bytes.length)throw new Error("intrinsics.npy has incomplete matrix data");var values=[],littleEndian=typeMatch[1]!==">";for(var i=0;i<9;i++)values.push(itemBytes===4?view.getFloat32(headerEnd+i*itemBytes,littleEndian):view.getFloat64(headerEnd+i*itemBytes,littleEndian));if(!values.every(Number.isFinite)||values[0]<=0||values[4]<=0)throw new Error("intrinsics.npy contains invalid focal lengths");return values}
async function loadCameraIntrinsics(file){if(!file)throw new Error("intrinsics.npy is not available for this depth method");var response=await fetch(file.url);if(!response.ok)throw new Error("Unable to load intrinsics.npy (HTTP "+response.status+")");return parseNpyMatrix3(await response.arrayBuffer())}
function applyCameraIntrinsics(camera,THREE,K,imageWidth,imageHeight,near,far){var fx=K[0],fy=K[4],cx=K[2],cy=K[5],left=-cx*near/fx,right=(imageWidth-cx)*near/fx,top=cy*near/fy,bottom=-(imageHeight-cy)*near/fy;camera.near=near;camera.far=far;camera.projectionMatrix.makePerspective(left,right,top,bottom,near,far,camera.coordinateSystem,camera.reversedDepth);camera.projectionMatrixInverse.copy(camera.projectionMatrix).invert()}
async function loadDepthPointCloud(stage,method){var host=byId("depth-ply-viewer"),meta=byId("depth-method-meta"),file=stageMethodFile(stage,method,"camera_pts.ply"),intrinsicsFile=stageMethodFile(stage,method,"intrinsics.npy");if(!host||!file)return;if(state.cleanup){state.cleanup();state.cleanup=null}meta.textContent=method+" · loading calibrated view";host.innerHTML='<div class="viewer-loading"><span class="spinner"></span><strong>Loading '+esc(method)+'…</strong></div>';try{var loaded=await Promise.all([Promise.all([import('../../../vendor/three/three.module.js'),import('../../../vendor/three/PLYLoader.js'),import('../../../vendor/three/OrbitControls.js')]),loadCameraIntrinsics(intrinsicsFile)]),modules=loaded[0],K=loaded[1],THREE=modules[0],PLYLoader=modules[1].PLYLoader,OrbitControls=modules[2].OrbitControls,geometry=await new PLYLoader().loadAsync(file.url);if(!document.body.contains(host)){geometry.dispose();return}var position=geometry.getAttribute("position"),imageWidth=state.review&&state.review.image.width,imageHeight=state.review&&state.review.image.height;if(!imageWidth||!imageHeight)throw new Error("RGB image dimensions are unavailable");geometry.computeBoundingBox();geometry.computeBoundingSphere();var width=Math.max(320,host.clientWidth),height=Math.max(320,host.clientHeight),renderer=new THREE.WebGLRenderer({antialias:true,alpha:false});renderer.setPixelRatio(Math.min(window.devicePixelRatio||1,2));renderer.setSize(width,height,false);renderer.setClearColor(0x080a0d,1);renderer.outputColorSpace=THREE.SRGBColorSpace;renderer.autoClear=false;host.replaceChildren(renderer.domElement);var scene=new THREE.Scene(),hasColors=Boolean(geometry.getAttribute("color")),radius=Math.max(geometry.boundingSphere&&geometry.boundingSphere.radius||1,.001),material=new THREE.PointsMaterial({size:Math.max(.002,radius/450),vertexColors:hasColors,color:hasColors?0xffffff:0xd7ff4f,sizeAttenuation:true}),points=new THREE.Points(geometry,material);scene.add(points);var minForward=Infinity,maxForward=0;for(var i=0;i<position.count;i++){var forward=-position.getZ(i);if(Number.isFinite(forward)&&forward>0){minForward=Math.min(minForward,forward);maxForward=Math.max(maxForward,forward)}}if(!Number.isFinite(minForward)||maxForward<=0)throw new Error("camera_pts.ply has no points in front of the camera");var cameraZ=1,orbitTargetZ=-1,near=Math.max(.0001,minForward*.05),far=Math.max(near*100,(maxForward+cameraZ)*2),camera=new THREE.PerspectiveCamera(45,imageWidth/imageHeight,near,far);applyCameraIntrinsics(camera,THREE,K,imageWidth,imageHeight,near,far);camera.position.set(0,0,cameraZ);camera.lookAt(0,0,orbitTargetZ);camera.updateMatrixWorld();var controls=new OrbitControls(camera,renderer.domElement);controls.target.set(0,0,orbitTargetZ);controls.minDistance=near*2;controls.maxDistance=far*.8;controls.update();meta.textContent=method+" · "+position.count.toLocaleString()+" points · calibrated "+imageWidth+"×"+imageHeight;var alive=true,renderWidth=width,renderHeight=height,observer=new ResizeObserver(function(){if(!alive)return;renderWidth=Math.max(1,host.clientWidth);renderHeight=Math.max(1,host.clientHeight);renderer.setSize(renderWidth,renderHeight,false)});observer.observe(host);(function loop(){if(!alive)return;controls.update();var viewport=containedImageRect(renderWidth,renderHeight,imageWidth,imageHeight,12),viewportX=Math.round(viewport.x),viewportY=Math.round(renderHeight-viewport.y-viewport.height),viewportWidth=Math.max(1,Math.round(viewport.width)),viewportHeight=Math.max(1,Math.round(viewport.height));renderer.setScissorTest(false);renderer.setViewport(0,0,renderWidth,renderHeight);renderer.clear(true,true,true);renderer.setViewport(viewportX,viewportY,viewportWidth,viewportHeight);renderer.setScissor(viewportX,viewportY,viewportWidth,viewportHeight);renderer.setScissorTest(true);renderer.render(scene,camera);requestAnimationFrame(loop)})();state.cleanup=function(){alive=false;observer.disconnect();controls.dispose();geometry.dispose();material.dispose();renderer.dispose();if(renderer.domElement.parentNode)renderer.domElement.remove()}}catch(e){host.innerHTML='<div class="viewer-empty error"><strong>Unable to load calibrated camera view</strong><span>'+esc(e.message)+'</span><a href="'+esc(file.url)+'" target="_blank" rel="noopener">Open source file</a></div>';meta.textContent=method+" · calibration unavailable"}}
function sceneGraphNodeColor(node){return node&&node.kind==="object"&&Number.isInteger(node.mask_index)?palette[node.mask_index%palette.length]:node&&node.id==="floor"?"#69737f":"#89939f"}
function sceneGraphLayout(nodes,edges){var byNode=new Map(nodes.map(function(n){return[n.id,n]})),supporterByChild=new Map(),supporters=new Set();edges.forEach(function(e){if(byNode.has(e.child)&&byNode.has(e.parent)&&e.child!==e.parent&&!supporterByChild.has(e.child)){supporterByChild.set(e.child,e.parent);supporters.add(e.parent)}});var depths=new Map();function depth(id,path){if(depths.has(id))return depths.get(id);if(path.has(id))return 0;var next=supporterByChild.get(id),value=next&&byNode.has(next)?depth(next,new Set(Array.from(path).concat(id)))+1:0;depths.set(id,value);return value}nodes.forEach(function(n){depth(n.id,new Set())});var connectedMax=Math.max.apply(null,[0].concat(Array.from(depths.values())));nodes.forEach(function(n){if(n.kind==="object"&&!supporterByChild.has(n.id)&&!supporters.has(n.id))depths.set(n.id,connectedMax+1)});var groups=new Map();nodes.forEach(function(n){var d=depths.get(n.id)||0;if(!groups.has(d))groups.set(d,[]);groups.get(d).push(n)});groups.forEach(function(group){group.sort(function(a,b){var ai=Number.isInteger(a.mask_index)?a.mask_index:Number.MAX_SAFE_INTEGER,bi=Number.isInteger(b.mask_index)?b.mask_index:Number.MAX_SAFE_INTEGER;return ai-bi||String(a.id).localeCompare(String(b.id))})});return{depths:depths,groups:groups,maxDepth:Math.max.apply(null,[0].concat(Array.from(groups.keys())))}}
function sceneGraphEdgePath(from,to,nodeWidth,nodeHeight){var x1=from.x,y1=from.y+nodeHeight/2,x2=to.x,y2=to.y-nodeHeight/2,curve=Math.max(22,Math.abs(y2-y1)*.42);return"M "+x1+" "+y1+" C "+x1+" "+(y1+curve)+", "+x2+" "+(y2-curve)+", "+x2+" "+y2}
function cloneSceneGraph(payload){return JSON.parse(JSON.stringify(payload))}
function updateSceneGraphZoom(){var host=byId("stage-graph"),svg=host&&host.querySelector(".scene-graph-svg"),label=byId("graph-zoom-reset"),out=byId("graph-zoom-out"),inside=byId("graph-zoom-in");if(svg){svg.style.width="100%";svg.style.minWidth="520px";svg.style.maxWidth="none";svg.style.transform="scale("+state.graphZoom+")";svg.style.transformOrigin="top center";svg.style.marginTop="0"}if(label)label.textContent=Math.round(state.graphZoom*100)+"%";if(out)out.disabled=state.graphZoom<=.5;if(inside)inside.disabled=state.graphZoom>=2}
function setSceneGraphZoom(value){var host=byId("stage-graph"),old=state.graphZoom,centerX=host?(host.scrollLeft+host.clientWidth/2)/old:0,top=host?host.scrollTop/old:0;state.graphZoom=Math.max(.5,Math.min(2,Math.round(value*10)/10));updateSceneGraphZoom();if(host){host.scrollLeft=Math.max(0,centerX*state.graphZoom-host.clientWidth/2);host.scrollTop=Math.max(0,top*state.graphZoom)}}
function setGraphDirty(value){state.graphDirty=value;var save=byId("save-scene-graph"),reset=byId("reset-scene-graph");if(save)save.disabled=!value;if(reset)reset.disabled=!value}
function sceneGraphHasCycle(payload){var parent=new Map();(payload.edges||[]).forEach(function(e){if(e.parent)parent.set(e.child,e.parent)});return Array.from(parent.keys()).some(function(start){var seen=new Set(),cursor=start;while(parent.has(cursor)){if(seen.has(cursor))return true;seen.add(cursor);cursor=parent.get(cursor)}return false})}
function selectSceneGraphNode(id){state.selectedGraphNodeId=id;drawSceneGraph(byId("stage-graph"),state.graphPayload)}
function renderSceneGraphEditor(){var host=byId("scene-graph-editor"),payload=state.graphPayload;if(!host||!payload)return;var node=(payload.nodes||[]).find(function(n){return n.id===state.selectedGraphNodeId});if(!node||node.kind!=="object"){host.innerHTML='<span>Select an object node to edit its name, relation, direct supporter and orientation.</span>';return}var edge=(payload.edges||[]).find(function(e){return e.child===node.id}),name=document.createElement("label"),supporter=document.createElement("label"),relation=document.createElement("label"),orientation=document.createElement("label"),nameInput=document.createElement("input"),supportSelect=document.createElement("select"),relationSelect=document.createElement("select"),orientationSelect=document.createElement("select");name.innerHTML="<span>Name</span>";nameInput.value=node.name||node.id;nameInput.setAttribute("aria-label","Object name");nameInput.onchange=function(){node.name=nameInput.value.trim()||node.id;var object=state.objects[node.mask_index];if(object)object.name=node.name;setGraphDirty(true);drawSceneGraph(byId("stage-graph"),payload)};name.append(nameInput);relation.innerHTML="<span>Relation</span>";["rests_on","fixed_to","supported_by"].forEach(function(value){relationSelect.append(new Option(value.replace(/_/g," "),value))});relationSelect.value=edge&&edge.relation||"rests_on";relationSelect.onchange=function(){if(!edge){edge={child:node.id,parent:null,relation:relationSelect.value,confidence:1,operational:false,unknown_reason:"unknown_supporter"};payload.edges.push(edge)}else edge.relation=relationSelect.value;setGraphDirty(true);drawSceneGraph(byId("stage-graph"),payload)};relation.append(relationSelect);supporter.innerHTML="<span>Direct Supporter</span>";supportSelect.append(new Option("Unknown / none",""));(payload.nodes||[]).filter(function(n){return n.id!==node.id}).forEach(function(n){supportSelect.append(new Option(String(n.name||n.id).replace(/_/g," "),n.id))});supportSelect.value=edge&&edge.parent||"";supportSelect.onchange=function(){var previous=edge&&edge.parent||"",next=supportSelect.value;if(edge){edge.parent=next||null;edge.operational=Boolean(next)}else if(next){edge={child:node.id,parent:next,relation:relationSelect.value,confidence:1,operational:true,unknown_reason:null};payload.edges.push(edge)}if(sceneGraphHasCycle(payload)){if(edge){edge.parent=previous||null;edge.operational=Boolean(previous)}supportSelect.value=previous;toast("A support relationship cannot form a cycle",true);return}setGraphDirty(true);drawSceneGraph(byId("stage-graph"),payload)};supporter.append(supportSelect);orientation.innerHTML="<span>Orientation</span>";[["auto","Auto (from support)"],["force","Always upright"],["free","Allow free orientation"]].forEach(function(item){orientationSelect.append(new Option(item[1],item[0]))});orientationSelect.value=node.upright_mode||"auto";orientationSelect.onchange=function(){node.upright_mode=orientationSelect.value;node.upright_reason="web_edit";setGraphDirty(true);drawSceneGraph(byId("stage-graph"),payload)};orientation.append(orientationSelect);host.replaceChildren(name,relation,supporter,orientation)}
function resetSceneGraph(){if(!state.graphOriginal)return;state.graphPayload=cloneSceneGraph(state.graphOriginal);state.selectedGraphNodeId=null;setGraphDirty(false);drawSceneGraph(byId("stage-graph"),state.graphPayload)}
async function saveSceneGraph(){if(!state.graphPayload||!state.graphDirty)return;var button=byId("save-scene-graph");button.disabled=true;button.textContent="Saving…";var base="/api/demo/cases/"+encodeURIComponent(state.caseId),saved=null,lastError=null;for(var path of [base+"/scene-graph",base+"/scene_graph"]){try{saved=await api(path,json("PUT",state.graphPayload));break}catch(e){lastError=e;if(!/404|Not Found/i.test(String(e.message||e)))break}}if(!saved){button.textContent="Save";button.disabled=false;toast(lastError?lastError.message:"Unable to save scene graph",true);return}state.graphPayload=saved;state.graphOriginal=cloneSceneGraph(saved);setGraphDirty(false);button.textContent="Saved";var freshUrl=(state.caseData&&state.caseData.scene_graph_url||"").split("?")[0];if(freshUrl)state.caseData.scene_graph_url=freshUrl+"?v="+Date.now();setTimeout(function(){if(document.body.contains(button))button.textContent="Save"},1200);toast("Scene graph saved — rerun Construct Scene to apply orientation changes")}
function drawSceneGraph(host,payload){var nodes=Array.isArray(payload.nodes)?payload.nodes.slice():[],allEdges=Array.isArray(payload.edges)?payload.edges.slice():[],byNode=new Map(nodes.map(function(n){return[n.id,n]})),edges=allEdges.filter(function(e){return byNode.has(e.child)&&byNode.has(e.parent)});if(!nodes.length){host.innerHTML='<div class="viewer-empty"><strong>No graph nodes available</strong><span>The scene_graph.json file does not contain any nodes.</span></div>';return}var layout=sceneGraphLayout(nodes,edges),nodeWidth=104,nodeHeight=36,hGap=24,vGap=56,padX=28,padY=26,maxInLayer=Math.max.apply(null,Array.from(layout.groups.values()).map(function(g){return g.length})),width=Math.max(360,padX*2+maxInLayer*nodeWidth+Math.max(0,maxInLayer-1)*hGap),height=Math.max(220,padY*2+(layout.maxDepth+1)*nodeHeight+layout.maxDepth*vGap),positions=new Map();layout.groups.forEach(function(group,depth){var total=group.length*nodeWidth+Math.max(0,group.length-1)*hGap,start=(width-total)/2,y=padY+(layout.maxDepth-depth)*(nodeHeight+vGap)+nodeHeight/2;group.forEach(function(n,i){positions.set(n.id,{x:start+i*(nodeWidth+hGap)+nodeWidth/2,y:y})})});var svg=document.createElementNS("http://www.w3.org/2000/svg","svg");svg.setAttribute("viewBox","0 0 "+width+" "+height);svg.setAttribute("role","img");svg.setAttribute("aria-label","Scene support graph. Supporters are below the objects they support.");svg.classList.add("scene-graph-svg");var defs=document.createElementNS(svg.namespaceURI,"defs"),marker=document.createElementNS(svg.namespaceURI,"marker");marker.setAttribute("id","support-arrow");marker.setAttribute("viewBox","0 0 10 10");marker.setAttribute("refX","9");marker.setAttribute("refY","5");marker.setAttribute("markerWidth","5");marker.setAttribute("markerHeight","5");marker.setAttribute("orient","auto-start-reverse");var arrow=document.createElementNS(svg.namespaceURI,"path");arrow.setAttribute("d","M 0 0 L 10 5 L 0 10 z");marker.append(arrow);defs.append(marker);svg.append(defs);var edgeGroup=document.createElementNS(svg.namespaceURI,"g");edgeGroup.classList.add("scene-graph-edges");edges.forEach(function(e){var from=positions.get(e.child),to=positions.get(e.parent),path=document.createElementNS(svg.namespaceURI,"path");path.setAttribute("d",sceneGraphEdgePath(from,to,nodeWidth,nodeHeight));path.setAttribute("marker-end","url(#support-arrow)");path.classList.toggle("non-operational",e.operational===false);var title=document.createElementNS(svg.namespaceURI,"title");title.textContent=(byNode.get(e.child).name||e.child)+" → "+(byNode.get(e.parent).name||e.parent)+" · "+(e.relation||"supported by")+(Number.isFinite(e.confidence)?" · "+Math.round(e.confidence*100)+"%":"");path.append(title);edgeGroup.append(path)});svg.append(edgeGroup);var nodeGroup=document.createElementNS(svg.namespaceURI,"g");nodeGroup.classList.add("scene-graph-nodes");nodes.forEach(function(n){var p=positions.get(n.id),g=document.createElementNS("http://www.w3.org/2000/svg","g"),rect=document.createElementNS(svg.namespaceURI,"rect"),label=document.createElementNS(svg.namespaceURI,"text"),meta=document.createElementNS(svg.namespaceURI,"text"),color=sceneGraphNodeColor(n),fullLabel=String(n.name||n.id).replace(/_/g," "),shortLabel=fullLabel.length>15?fullLabel.slice(0,14)+"…":fullLabel,orientation=n.kind==="object"?(n.upright_mode||"auto").toUpperCase():"";g.setAttribute("transform","translate("+(p.x-nodeWidth/2)+" "+(p.y-nodeHeight/2)+")");g.classList.add("scene-graph-node");if(n.kind!=="object")g.classList.add("environment-node");if(n.id===state.selectedGraphNodeId)g.classList.add("selected");if(n.kind==="object"){g.setAttribute("tabindex","0");g.setAttribute("role","button");g.onclick=function(){selectSceneGraphNode(n.id)};g.onkeydown=function(e){if(e.key==="Enter"||e.key===" "){e.preventDefault();selectSceneGraphNode(n.id)}}}rect.setAttribute("width",nodeWidth);rect.setAttribute("height",nodeHeight);rect.setAttribute("rx","7");rect.setAttribute("fill",n.kind==="object"?color:"#171c22");rect.setAttribute("fill-opacity",n.kind==="object"?"0.22":"1");rect.setAttribute("stroke",color);label.setAttribute("x",nodeWidth/2);label.setAttribute("y",15);label.textContent=shortLabel;meta.setAttribute("x",nodeWidth/2);meta.setAttribute("y",28);meta.classList.add("scene-graph-node-meta");meta.textContent=n.kind==="object"&&Number.isInteger(n.mask_index)?"MASK "+String(n.mask_index).padStart(3,"0")+" · "+orientation:(n.kind==="object"?"OBJECT":n.id==="floor"?"SUPPORT SURFACE":"STATIC ANCHOR");var title=document.createElementNS(svg.namespaceURI,"title");title.textContent=fullLabel+(n.motion?" · "+n.motion:"")+(orientation?" · orientation "+orientation.toLowerCase():"");g.append(rect,label,meta,title);nodeGroup.append(g)});svg.append(nodeGroup);host.replaceChildren(svg);var legend=document.createElement("div");legend.className="scene-graph-legend";legend.innerHTML='<span><i class="graph-line"></i>supported by</span><span><i class="graph-level"></i>supporters below</span>';host.append(legend);updateSceneGraphZoom();renderSceneGraphEditor()}
async function renderSceneGraph(){var host=byId("stage-graph"),url=state.caseData&&state.caseData.scene_graph_url;if(!host)return;if(!url){host.innerHTML='<div class="viewer-empty"><strong>Scene graph not available</strong><span>This case has no scene_graph.json output.</span></div>';return}var requestUrl=url+(url.indexOf("?")>=0?"&":"?")+"v="+Date.now(),active=true;state.graphCleanup=function(){active=false};try{var payload=await api(requestUrl,{cache:"no-store"});if(active&&document.body.contains(host)){state.graphPayload=payload;state.graphOriginal=cloneSceneGraph(payload);state.selectedGraphNodeId=null;setGraphDirty(false);drawSceneGraph(host,payload)}}catch(e){if(active)host.innerHTML='<div class="viewer-empty error"><strong>Unable to load scene graph</strong><span>'+esc(e.message)+'</span><a href="'+esc(url)+'" target="_blank" rel="noopener">Open source file</a></div>'}}
function maskPreview(){var target=byId("stage-mask-preview"),masks=state.caseData.masks||[];if(!target||!masks.length)return;var c=document.createElement("canvas");c.width=518;c.height=518;var x=c.getContext("2d"),left=masks.length;masks.forEach(function(m,i){var im=new Image();im.onload=function(){var one=document.createElement("canvas");one.width=im.naturalWidth;one.height=im.naturalHeight;var ox=one.getContext("2d");ox.drawImage(im,0,0);var p=ox.getImageData(0,0,one.width,one.height),rgb=palette[i%palette.length].match(/[\da-f]{2}/gi).map(function(v){return parseInt(v,16)});for(var j=0;j<p.data.length;j+=4)if(p.data[j]>127){p.data[j]=rgb[0];p.data[j+1]=rgb[1];p.data[j+2]=rgb[2];p.data[j+3]=200}ox.putImageData(p,0,0);x.drawImage(one,0,0,518,518);if(!--left)target.src=c.toDataURL()};im.src=m.url})}
async function render3d(){var host=byId("stage-scene-viewer"),methods=(state.caseData.scene_methods||[]).filter(function(m){return /_depth$/i.test(m.method)});methods=methods.map(function(m){var copy=Object.assign({},m);if(!copy.scene_with_floor_url&&copy.scene_url)copy.scene_with_floor_url=copy.scene_url.replace(/scene\.glb(?:\?.*)?$/i,"scene_with_floor.glb");return copy});var floorMethods=methods.filter(function(m){return m.scene_with_floor_url});if(!host||!methods.length){if(host)host.innerHTML='<div class="viewer-empty"><strong>Depth scene not available</strong><span>This case has no constructed *_depth scene.</span></div>';return}if(state.cleanup){state.cleanup();state.cleanup=null}if(!floorMethods.length){host.innerHTML='<div class="viewer-empty"><strong>Floor scene not available</strong><span>This case has no scene_with_floor.glb output under a *_depth method.</span></div>';return}var select=document.createElement("select");select.setAttribute("aria-label","Scene method");floorMethods.forEach(function(m){select.append(new Option(m.method,m.method))});host.replaceChildren(select);var holder=document.createElement("div");holder.className="scene-viewer-canvas";host.append(holder);async function show(){if(state.cleanup){state.cleanup();state.cleanup=null}var method=floorMethods.find(function(m){return m.method===select.value});holder.innerHTML='<div class="viewer-loading"><span class="spinner"></span><strong>Loading '+esc(select.value)+'…</strong></div>';try{if(!state.three)state.three=await Promise.all([import('../../../vendor/three/three.module.js'),import('../../../vendor/three/GLTFLoader.js'),import('../../../vendor/three/OrbitControls.js')]);var THREE=state.three[0],GLTFLoader=state.three[1].GLTFLoader,OrbitControls=state.three[2].OrbitControls,model=await new GLTFLoader().loadAsync(method.scene_with_floor_url);if(!document.body.contains(holder))return;var sourceMaterials=[];model.scene.traverse(function(object){if(!object.isMesh||!object.material)return;var originals=Array.isArray(object.material)?object.material:[object.material];var basics=originals.map(function(material){sourceMaterials.push(material);var basic=new THREE.MeshBasicMaterial({color:material.color?material.color.clone():new THREE.Color(0xffffff),map:material.map||null,alphaMap:material.alphaMap||null,transparent:Boolean(material.transparent),opacity:material.opacity===undefined?1:material.opacity,alphaTest:material.alphaTest||0,side:material.side===undefined?THREE.FrontSide:material.side,vertexColors:Boolean(material.vertexColors)});basic.name=material.name||"texture-only";return basic});object.material=Array.isArray(object.material)?basics:basics[0]});var w=Math.max(1,holder.clientWidth||400),h=Math.max(1,holder.clientHeight||320),r=new THREE.WebGLRenderer({antialias:true});r.setPixelRatio(Math.min(window.devicePixelRatio||1,2));r.setSize(w,h,false);r.outputColorSpace=THREE.SRGBColorSpace;holder.replaceChildren(r.domElement);var scene=new THREE.Scene();scene.background=new THREE.Color(0x101318);var environmentTexture=null,environmentUrl=state.caseData.environment_url;if(environmentUrl){try{environmentTexture=await new THREE.TextureLoader().loadAsync(environmentUrl);environmentTexture.mapping=THREE.EquirectangularReflectionMapping;environmentTexture.colorSpace=THREE.SRGBColorSpace;scene.background=environmentTexture;state.stage2EnvironmentScene=scene;applyEnvironmentRotation()}catch(_){environmentTexture=null}}scene.add(model.scene);var cam=new THREE.PerspectiveCamera(stage2ViewerDefaults.cameraFovDegrees,w/h,.01,1000),controls=new OrbitControls(cam,r.domElement);var box=new THREE.Box3().setFromObject(model.scene),center=box.getCenter(new THREE.Vector3()),size=box.getSize(new THREE.Vector3()),radius=Math.max(size.x,size.y,size.z,.5);controls.target.copy(center);cam.position.copy(center).add(new THREE.Vector3(radius*stage2ViewerDefaults.cameraOffsetRadius.x,radius*stage2ViewerDefaults.cameraOffsetRadius.y,radius*stage2ViewerDefaults.cameraOffsetRadius.z));cam.near=radius*stage2ViewerDefaults.nearRadiusScale;cam.far=radius*stage2ViewerDefaults.farRadiusScale;cam.updateProjectionMatrix();controls.update();var alive=true,observer=new ResizeObserver(function(){if(!alive)return;var width=Math.max(1,holder.clientWidth),height=Math.max(1,holder.clientHeight);cam.aspect=width/height;cam.updateProjectionMatrix();r.setSize(width,height,false)});observer.observe(holder);(function loop(){if(!alive)return;controls.update();r.render(scene,cam);requestAnimationFrame(loop)})();state.cleanup=function(){alive=false;if(state.stage2EnvironmentScene===scene)state.stage2EnvironmentScene=null;observer.disconnect();controls.dispose();if(environmentTexture)environmentTexture.dispose();model.scene.traverse(function(object){if(object.geometry)object.geometry.dispose();if(object.material){var materials=Array.isArray(object.material)?object.material:[object.material];materials.forEach(function(material){material.dispose()})}});sourceMaterials.forEach(function(material){material.dispose()});r.dispose();if(r.domElement.parentNode)r.domElement.remove()}}catch(e){holder.innerHTML='<div class="viewer-empty error"><strong>Unable to load floor scene</strong><span>'+esc(e.message)+'</span><a href="'+esc(method.scene_with_floor_url)+'" target="_blank" rel="noopener">Open source file</a></div>'}}select.onchange=show;show()}
function renderStage(){renderPipeline();document.body.dataset.stage=String(state.stage);byId("stageMeta").textContent="Stage "+state.stage+" · "+stageNames[state.stage];var lastStage=stageNames.length-1;byId("next-stage").disabled=!state.caseId||state.stage>=lastStage;byId("next-stage").textContent=state.stage>=lastStage?"Pipeline Complete":"Next Stage →";byId("save-review").classList.toggle("hidden",state.stage!==0);byId("stage-info-panel").classList.toggle("hidden",state.stage===0);var ccmButton=byId("run-inference-ccm"),meshButton=byId("run-inference-mesh"),constructButton=byId("construct-scene-action");if(ccmButton)ccmButton.hidden=state.stage!==1;if(meshButton)meshButton.hidden=state.stage!==1;if(constructButton)constructButton.hidden=state.stage!==2;var constructOptionsPanel=byId("construct-options-panel");if(constructOptionsPanel)constructOptionsPanel.hidden=state.stage!==2;renderPipelineJob();var s=state.caseData&&state.caseData.stages&&state.caseData.stages[state.stage];if(state.stage>0)byId("result-summary").textContent=s&&s.ready?(s.summary+" available"):"No result for this stage yet.";byId("stage-content").innerHTML=detailsHtml(s);if(state.stage===0){clearStageViewers();byId("result-stage").classList.add("hidden");byId("canvas-toolbar").classList.remove("hidden");canvas.style.display=state.review?"block":"none";if(state.review)draw()}else{byId("canvas-toolbar").classList.add("hidden");canvas.style.display="none";byId("emptyStage").classList.add("hidden");renderResultStage()}}
function pipelineJobActive(){return Boolean(state.pipelineJob&&/queued|running|cancelling/.test(state.pipelineJob.status))}
function renderPipelineJob(){var box=byId("pipeline-job"),active=pipelineJobActive();["run-inference-ccm","run-inference-mesh","construct-scene-action"].forEach(function(id){var button=byId(id);if(button)button.disabled=active});setupConstructOptionsPanel();if(!box)return;var job=state.pipelineJob;box.hidden=!job;if(!job)return;box.className="pipeline-job "+(job.status||"");byId("pipeline-job-title").textContent=(job.action||"Pipeline task").replace(/_/g," ");byId("pipeline-job-percent").textContent=Math.round(Number(job.progress||0)*100)+"%";byId("pipeline-job-progress").style.width=Math.round(Number(job.progress||0)*100)+"%";byId("pipeline-job-step").textContent=job.current_step?(job.current_step+" · "+job.status):job.status;byId("cancel-pipeline-job").hidden=!active;byId("cancel-pipeline-job").disabled=job.status==="cancelling"}
function stopPipelinePolling(){if(state.pipelinePoll){clearTimeout(state.pipelinePoll);state.pipelinePoll=null}}
async function pollPipelineJob(){var job=state.pipelineJob;if(!job)return;var jobId=job.id,caseId=state.caseId;try{var updated=await api("/api/demo/jobs/"+encodeURIComponent(jobId),{cache:"no-store"});var log=await api("/api/demo/jobs/"+encodeURIComponent(jobId)+"/log?offset="+state.pipelineLogOffset,{cache:"no-store"});if(!state.pipelineJob||state.pipelineJob.id!==jobId||state.caseId!==caseId)return;state.pipelineJob=updated;if(log.text){var node=byId("pipeline-job-log");node.textContent+=log.text;node.scrollTop=node.scrollHeight}state.pipelineLogOffset=log.offset;renderPipelineJob();if(pipelineJobActive()||!log.complete){state.pipelinePoll=setTimeout(pollPipelineJob,pipelineJobActive()?1000:100);return}stopPipelinePolling();if(state.pipelineJob.status==="succeeded"){toast("Pipeline task completed");await refreshCurrentCase()}else if(state.pipelineJob.status==="failed")toast(state.pipelineJob.error||"Pipeline task failed",true);else if(state.pipelineJob.status==="cancelled")toast("Pipeline task cancelled",true)}catch(e){if(state.pipelineJob&&state.pipelineJob.id===jobId&&state.caseId===caseId){stopPipelinePolling();toast(e.message,true)}}}
async function refreshCurrentCase(){if(!state.caseId)return;var current=state.stage;try{state.caseData=normalizeCaseData(await api("/api/demo/cases/"+encodeURIComponent(state.caseId)+"?v="+Date.now(),{cache:"no-store"}));await loadReview();state.stage=current;renderStage()}catch(e){toast("Unable to refresh case results: "+e.message,true)}}
async function submitPipelineAction(action){if(!state.caseId){toast("Select a case first",true);return}if(pipelineJobActive()){toast("A pipeline task is already running",true);return}try{var payload={action:action};if(action==="construct_scene")payload.options=Object.assign({},state.constructOptions);state.pipelineJob=await api("/api/demo/cases/"+encodeURIComponent(state.caseId)+"/jobs",json("POST",payload));state.pipelineLogOffset=0;byId("pipeline-job-log").textContent="";renderPipelineJob();stopPipelinePolling();pollPipelineJob()}catch(e){toast(e.message,true)}}
async function restorePipelineJob(){if(!state.caseId)return;var caseId=state.caseId;try{var response=await api("/api/demo/cases/"+encodeURIComponent(caseId)+"/jobs/active",{cache:"no-store"});if(state.caseId!==caseId)return;if(response.job){state.pipelineJob=response.job;state.pipelineLogOffset=0;byId("pipeline-job-log").textContent="";renderPipelineJob();stopPipelinePolling();pollPipelineJob()}}catch(_){}}
async function openCase(caseId,keepUrl){stopPipelinePolling();state.pipelineJob=null;state.caseId=caseId;loadEnvironmentRotation(caseId);setSystem("Loading","running");try{state.caseData=normalizeCaseData(await api("/api/demo/cases/"+encodeURIComponent(caseId)+"?v="+Date.now(),{cache:"no-store"}));document.querySelectorAll(".case-item").forEach(function(b){b.setAttribute("aria-selected",b.dataset.caseId===caseId?"true":"false")});byId("canvasMeta").textContent=caseId;await loadReview();if(!keepUrl)updateUrl(false);renderStage();await restorePipelineJob();setSystem("Ready","ready")}catch(e){setSystem("Failed","failed");toast(e.message,true)}}
function showCaseLanding(){stopPipelinePolling();clearStageViewers();state.pipelineJob=null;state.pipelineLogOffset=0;state.caseId=null;state.caseData=null;state.stage=0;state.review=null;state.image=null;state.objects=[];state.selectedId=null;state.view=null;document.body.dataset.stage="0";setSelectedMaskPaneVisible(false);canvas.style.display="none";byId("result-stage").classList.add("hidden");byId("canvas-toolbar").classList.remove("hidden");byId("emptyStage").classList.remove("hidden");byId("stage-info-panel").classList.add("hidden");byId("save-review").classList.remove("hidden");byId("save-review").disabled=true;byId("next-stage").disabled=true;byId("next-stage").textContent="Next Stage →";byId("canvasMeta").textContent="No scene loaded";byId("stageMeta").textContent="Stage 0 · Segmentation";byId("result-summary").textContent="Select a case to begin.";byId("object-count").textContent="0";byId("review-progress").textContent="0 / 0 reviewed";byId("object-list").innerHTML='<div class="empty-small">Objects will appear here after segmentation</div>';renderPipelineJob();renderPipeline();renderCases();setSystem("Select a case","")}
async function upload(file){if(!file)return;var reader=new FileReader();reader.onload=async function(){try{setSystem("Uploading","running");var r=await api("/api/demo/cases/resolve",json("POST",{filename:file.name,image_data:String(reader.result)})),d=await api("/api/demo/cases");state.cases=d.cases||[];renderCases();await openCase(r.case_id,false)}catch(e){toast(e.message,true)}};reader.readAsDataURL(file)}
document.querySelectorAll(".tool").forEach(function(b){b.onclick=function(){setTool(b.dataset.tool)}});byId("opacity").oninput=function(){state.opacity=Number(this.value)/100;draw()};byId("brush-size").oninput=function(){state.brushSize=Number(this.value);byId("brush-size-value").textContent=this.value+" px";updatePaintCursor()};byId("undo-paint").onclick=undo;byId("redo-paint").onclick=redo;byId("clear-prompts").onclick=function(){resetPrompts();draw()};byId("save-review").onclick=saveReview;byId("next-stage").onclick=function(){if(!state.caseId||state.stage>=stageNames.length-1)return;state.stage+=1;updateUrl(false);renderStage()};byId("run-inference-ccm").onclick=function(){submitPipelineAction("inference_ccm")};byId("run-inference-mesh").onclick=function(){submitPipelineAction("inference_mesh")};byId("construct-scene-action").onclick=function(){submitPipelineAction("construct_scene")};byId("cancel-pipeline-job").onclick=async function(){if(!state.pipelineJob)return;try{state.pipelineJob=await api("/api/demo/jobs/"+encodeURIComponent(state.pipelineJob.id),{method:"DELETE"});renderPipelineJob()}catch(e){toast(e.message,true)}};byId("add-by-text").onclick=addText;byId("add-prompt-mask").onclick=addEmpty;byId("toggle-add-object").onclick=function(){var p=byId("add-object-panel"),show=p.hidden;p.hidden=!show;this.setAttribute("aria-expanded",show?"true":"false")};byId("finish-mask").onclick=function(){resetPrompts();var i=state.objects.findIndex(function(o){return o.id===state.selectedId});if(state.objects.length)selectObject(state.objects[(i+1)%state.objects.length].id)};byId("text-prompt").onkeydown=function(e){if(e.key==="Enter")addText()};byId("zoom-out").onclick=function(){setCanvasZoom(state.zoom/1.25)};byId("zoom-in").onclick=function(){setCanvasZoom(state.zoom*1.25)};byId("reset-zoom").onclick=resetCanvasView;byId("case-upload-btn").onclick=function(){toast("暂不支持上传，请先使用下面的预处理结果。")};byId("case-upload").disabled=true;byId("case-upload-btn").ondragover=function(e){e.preventDefault()};byId("case-upload-btn").ondrop=function(e){e.preventDefault();toast("暂不支持上传，请先使用下面的预处理结果。")};
window.addEventListener("keydown",function(e){if(e.code==="Space"&&!/INPUT|TEXTAREA/.test(document.activeElement.tagName)){state.spacePressed=true;e.preventDefault()}});
window.addEventListener("keyup",function(e){if(e.code==="Space")state.spacePressed=false});
window.addEventListener("resize",draw);
window.onpopstate=function(){var r=route();if(!r.caseId){showCaseLanding();return}state.stage=r.stage;if(r.caseId!==state.caseId)openCase(r.caseId,true);else renderStage()};
(async function(){try{var r=route(),d=await api("/api/demo/cases"),hasRequestedCase=false;state.cases=d.cases||[];hasRequestedCase=Boolean(r.caseId&&state.cases.some(function(c){return c.case_id===r.caseId}));state.stage=hasRequestedCase?r.stage:0;state.caseId=hasRequestedCase?r.caseId:null;renderPipeline();renderCases();if(state.caseId)await openCase(state.caseId,true);else showCaseLanding()}catch(e){setSystem("Failed","failed");toast(e.message,true)}})();
})();
